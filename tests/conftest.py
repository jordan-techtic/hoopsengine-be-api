EXISTING — no changes required. Already provides PostgreSQL TestClient, seeded_users (admin/regular/viewer/inactive + new user data), auth_headers, ensure_practice_plans_table, ensure_teams_table, and org-admin API base URL constants.